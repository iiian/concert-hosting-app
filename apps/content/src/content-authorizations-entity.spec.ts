import { ContentAuthorizationsEntity } from './content-authorizations-entity';

describe('ContentAuthorizationsEntity', () => {
  it('should be defined', () => {
    expect(new ContentAuthorizationsEntity()).toBeDefined();
  });
});
